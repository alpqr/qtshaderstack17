#version 100

struct buf
{
    mat4 mvp;
};

uniform buf ubuf;

varying vec3 v_color;
attribute vec3 color;
attribute vec4 position;

void main()
{
    v_color = color;
    gl_Position = ubuf.mvp * position;
}

